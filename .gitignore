# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
libs_backup
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
.venv/
venv/
ENV/

# IDEs
.vscode/
.idea/

# Project Specific
logs/*.log
data/*.db*
data/*.sqlite3*
data/browser-profile/
data/_archive_*/
data/chroma/
data/media/
data/memory/
data/projects/
data/uploads/
# P2.2: canonical DB is data/namma_agent.db; core/data/ was the accidental duplicate
core/data/
models/
piper/
# Redistributable Piper binary tarball (~26MB, re-downloaded by setup)
piper_linux_x86_64.tar.gz
tmp_plan_folder_ctx*

# Testing
.pytest_cache/
.coverage
htmlcov/

# OS
.DS_Store
Thumbs.db

#logs
logs/*.log
logs/
#env
.env
.env.cognee
.env.cognee.cloud

scratch/*
# Claude Code ephemeral worktrees (never commit)
.claude/worktrees/

# Generated RAG knowledge graph — rebuild locally with /graphify (~80 MB, regenerable)
graphify-out/

# AI-interaction dev logs — written locally per CLAUDE.md, kept out of the public repo
responses/
plan/

# pywebview disk cache (warm-start cache for the native desktop window)
namma_agent/.webview/

# Ad-hoc scratch notes (not part of the project)
notes.txt
notes.md
namma_agent_notes.md
full_after.txt

# Loose RAG test PDFs dropped at repo root (real fixtures live under tests/)
/RAG Testing Dataset_ Aerospace Engineering.pdf
/RAG Testing File Generation - Google Gemini.pdf

# Runtime state the app rewrites (regenerated on launch — never commit)
data/*.json

# Secrets backups (e.g. produced during an env-var migration)
.env.bak
.env.*.bak
*.env.bak

# Local config overlay — machine-specific (assistant name, provider keys, persona).
# Must NEVER be committed/shipped: it was leaking a dev's `name: Aria` into installs.
config.local.yaml
namma_agent/config.local.yaml

# Desktop installer / packaging build output
installers/build/
installers/dist/
installers/native/build/
installers/native/dist/
*.AppImage
*.dmg
*.spec
NammaAgent*-Setup-*.exe
NammaAgentInstaller*.exe

# Loose scratch dropped at repo root (portfolio/blog/render experiments)
/chunk*.js
/style*.css
/*-mmd.py
/auth_flow_*.png

# Website Node_Modules
website/node_modules